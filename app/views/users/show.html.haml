%table.profile-table
  %tr
    %th ニックネーム
    %td= @user.nickname
  %tr
    %th 郵便番号
    %td= @user.postnumber

%div
  %a.body-button{ :href => edit_user_path(params[:id]) } プロフィール編集

%div.question-list
  - @questions.each do |question|
    %div.question-block
      %div.question-content1
        %h4= question.title
        %p= question.content
        %div.create-time
          = question.created_at.strftime('%Y年%m月%d日 %H:%M')
        %div.count-display
          %a.body-button{ :href => question_path(question) } 回答数：#{question.answers.size}
        %div.action-button
          %a{ :href => edit_question_path(question) } 編集する
          %a{ :href => question_path(question), :"data-method" => :delete} 削除する