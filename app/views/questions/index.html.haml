%div#map
:javascript
  var map;
  function initMap() {
    map = new google.maps.Map(document.getElementById('map'), {
      center: {lat: 35.0258, lng: 135.761},
      zoom: 14
    });
  }
%script{:async => "", :defer => "defer", :src => "https://maps.googleapis.com/maps/api/js?key=AIzaSyADmBdh0GEa0VvbPYq0DD-CxCX6V8wzJmI&callback=initMap"}

%div.question-list
  - @questions.each do |question|
    %div.question-block
      %div.question
        %h4= question.title
        %p= question.content.truncate(10)
        %div.create-time
          投稿時間
          = question.created_at.strftime('%Y年%m月%d日 %H:%M')
        %div.create-person
          投稿者
          = question.user.nickname
      %div
        = button_to "回答数: #{question.answers.size}", question_path(question), method: :get
      %div
        - if question.user_id == current_user.id
          = button_to "編集する", edit_question_path(question), method: :get
        - else
          = button_to "答える", new_question_answer_path(question), method: :get